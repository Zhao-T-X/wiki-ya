# pnpm 10+ 将「允许执行安装脚本的依赖」收敛到 workspace 配置。
# esbuild 需要 postinstall 落地平台二进制，否则 Vite 无法启动。
# pnpm 11 起以 allowBuilds 显式声明（onlyBuiltDependencies 保留以兼容 10.x）。
allowBuilds:
  esbuild: true
onlyBuiltDependencies:
  - esbuild
