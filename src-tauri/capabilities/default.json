{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口的默认权限：仅开放核心能力，知识库读写一律经 Rust commands。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
